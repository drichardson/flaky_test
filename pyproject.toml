[project]
name = "flaky-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=8.3.4",
]

[tool.pytest.ini_options]
addopts = "--strict-markers"
markers = [
	"flaky: flaky test marker"
]
